Entities:

User, Driver, Ride, Cab

APIs:

Driver:

/driver/register -> need to provide driver name, cab number, type, capcaity,

/driver/updateStatus -> update status of driver

/driver/accept -> pickup, drop , cost,

/driver/reject -> pickup, drop , cost,

/driver/getRating

User:

/user/register -> name, password

/user/bookCab -> pickup, drop

/user/cancelRide -> cancel ride id

/user/getRides

/user/rateDriver